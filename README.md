# Medical Prototype Simple

A new standalone project inspired by the provided Figma/React prototype.

## Run

- Open `index.html` directly in your browser.

## Notes

- This project is fully separate from the original `Medical Diagnostic Center Landing Page` folder.
- Structure kept simple: `index.html`, `styles.css`, `script.js`.
